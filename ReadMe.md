# docker-web-netcore
---------------------------------
# build with no cache
docker-compose build --no-cache
---------------------------------
# start the services
docker-compose up
---------------------------------
# list the services
docker-compose ps
---------------------------------
# list the containers
docker ps
---------------------------------
# stop services
docker-compose stop
----------------------------------
